# Data Configuration
data:
  raw_path: "data/raw"
  processed_path: "data/processed"
  
preprocessing:
  remove_special_chars: true
  lowercase: true
  remove_stopwords: true
  
splitting:
  train_ratio: 0.7
  val_ratio: 0.15
  test_ratio: 0.15
  random_seed: 42
